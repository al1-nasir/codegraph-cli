# Sample project for testing CodeGraph CLI

This is a minimal Python project used for testing the parser, indexing, and graph operations.

## Structure

- `main.py` - Entry point with imports
- `utils.py` - Utility functions
- `models.py` - Data classes
- `processor.py` - Business logic that calls utils
